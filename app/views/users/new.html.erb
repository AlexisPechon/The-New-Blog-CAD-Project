<h1>Create an account</h1>

<%# The following file contains the necessary code to generate the input field for the user %>
<%# to input their username and password %>

<%# This file is also responsible for generating the Sign Up form for the user %>

<%= form_with(model: @user, url: users_path) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %> <%# Creates a field for the email %>

  <%= f.label :password %>
  <%= f.password_field :password %> <%# Creates a field for the password %>

  <%= f.label :password_confirmation %>
  <%= f.password_field :password_confirmation %> <%# Creates a field for the password confirmation %>

  <%= f.submit "Create Account" %>
<% end %>
