<%# The following file contains the logic necessary to display the different articles/games
<%# that were created by the user on the main page of the website.%>

<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<ul>
  <li>
    <%= link_to "Edit", edit_article_path(@article) %>
  </li>
  <li>
    <%= link_to "Destroy", article_path(@article), data:{
      turbo_method: :delete, turbo_confirm: "Are you sure?"
    } %>
  </li>
</ul>
<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a commment</h2>
<%= render 'comments/form' %>


<hr>

<%#
<%= link_to "Back Home", articles_path %>

<%# <div class = "backHome">
<%# <%= button_to "Back Home", articles_path, method: :get, class: "btn btn-primary" %>
</div>
